services:
  - type: web
    name: diesel-dude-app
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: NODE_OPTIONS='--max-old-space-size=400' npm start
    envVars:
      - key: NODE_ENV
        value: production
